# ==============================================
# SMART PORTAL CONFIGURATION
# Copy this file to .env and update the values
# ==============================================

# --- Database Settings ---
# The credentials for the PostgreSQL container
DB_USER=portal_admin
DB_PASS=REPLACE_WITH_SECURE_PASSWORD
DB_NAME=smart_portal
DB_HOST=127.0.0.1
DB_PORT=5432

# --- Web Application Secrets ---
# A random string used to encrypt user sessions
SESSION_SECRET=REPLACE_WITH_LONG_RANDOM_STRING

# --- Network Configuration ---
# The IP address the Pi will assign to itself
GATEWAY_IP=192.168.10.1

# The domain name for the portal (e.g., home.local or login.wifi)
DOMAIN=portal.wifi

# The Wi-Fi interface name (usually wlan0 on Raspberry Pi)
WLAN_IFACE=wlan0

# --- Initial Admin Account ---
# Used by init_db.js to create the first user
ADMIN_USER=admin
ADMIN_PASS=REPLACE_WITH_ADMIN_PASSWORD