<!DOCTYPE html>
<html>

<head>
    <title>Guest Access</title>
    <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet'>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            background: linear-gradient(135deg, #667eea, #764ba2);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            font-family: sans-serif;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            background: rgba(255, 255, 255, 0.95);
            width: 100%;
            max-width: 400px;
            padding: 30px;
        }
    </style>
</head>

<body>
    <div class="card text-center">
        <h3 class="mb-3 text-info fw-bold">Guest Portal</h3>
        <% if(message){ %>
            <div class="alert alert-warning py-2 small">
                <%= message %>
            </div>
            <% } %>
                <form action="/guest_login" method="POST">
                    <div class="form-floating mb-2"><input type="text" name="name" class="form-control" id="gn"
                            placeholder="Name" required><label for="gn">Your Name</label></div>
                    <div class="form-floating mb-3"><input type="text" name="code" class="form-control" id="gc"
                            placeholder="Code" required><label for="gc">Voucher Code</label></div>
                    <div class="form-check text-start mb-3 ms-2"><input class="form-check-input" type="checkbox"
                            required id="tc"><label class="form-check-label small text-muted" for="tc">I agree to
                            acceptable use policy.</label></div><button
                        class="btn btn-info text-white w-100 rounded-pill py-2 fw-bold">Redeem Voucher</button>
                </form>
                <div class="mt-3"><a href="/" class="text-decoration-none text-muted">Back to Login</a></div>
    </div>
</body>

</html>