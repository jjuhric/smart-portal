<!DOCTYPE html>
<html>

<head>
    <title>Login</title>
    <link href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet'>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            background: linear-gradient(135deg, #667eea, #764ba2);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            font-family: sans-serif;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            background: rgba(255, 255, 255, 0.95);
            width: 100%;
            max-width: 400px;
            padding: 30px;
        }
    </style>
</head>

<body>
    <div class="card text-center">
        <h3 class="mb-3 text-primary fw-bold">Uhrick Wi-Fi</h3>
        <% if(message){ %>
            <div class="alert alert-warning py-2 small">
                <%= message %>
            </div>
            <% } %>
                <form action="/login" method="POST">
                    <div class="form-floating mb-2"><input type="text" name="username" class="form-control" id="u"
                            placeholder="User"><label for="u">Username</label></div>
                    <div class="form-floating mb-3"><input type="password" name="password" class="form-control" id="p"
                            placeholder="Pass"><label for="p">Password</label></div><button
                        class="btn btn-primary w-100 rounded-pill py-2 fw-bold">CONNECT</button>
                </form>
                <div class="d-flex align-items-center my-3">
                    <hr class="flex-grow-1"><span class="mx-2 text-muted small">OR</span>
                    <hr class="flex-grow-1">
                </div><a href="/guest" class="btn btn-dark w-100 rounded-pill mb-3">Guest Access</a>
                <div class="mt-2"><a href="/register" class="text-decoration-none fw-bold">Create Account</a></div>
    </div>
</body>

</html>